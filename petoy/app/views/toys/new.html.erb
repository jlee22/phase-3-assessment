<% if @errors %>
  <% @errors.each do |err| %>
    <p><%= err %></p>
  <% end %>
<% end %>

<%= form_for [:pet, @toy] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %><br>

  <%= f.submit %>
<% end %>
